apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: gateway
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: REPLACEME
    http:
      paths:
      - path: /v1
        backend:
          servicePort: http
          serviceName: gateway
